<%= form_for(@modull) do |f| %>
  <% if @modull.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modull.errors.count, "error") %> prohibited this modull from being saved:</h2>

      <ul>
      <% @modull.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
